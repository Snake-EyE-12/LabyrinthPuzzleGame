using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScreenSizeSetter : MonoBehaviour
{
    // Start is called before the first frame update
    // void Start()
    // {
    //     Screen.SetResolution(Screen.width, (int)(Screen.width * (16f / 9f)), false);
    // }
    //
    // // Update is called once per frame
    // private float lastWidth;
    // private float lastHeight;
    //
    // void Update()
    // {
    //     if(lastWidth != Screen.width)
    //     {
    //         Screen.SetResolution(Screen.width, (int)(Screen.width * (16f / 9f)), false);
    //     }
    //     else if(lastHeight != Screen.height)
    //     {
    //         Screen.SetResolution((int)(Screen.height * (9f / 16f)), Screen.height, false);
    //     }
    //
    //     lastWidth = Screen.width;
    //     lastHeight = Screen.height;
    // }
}
